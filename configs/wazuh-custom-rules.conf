
<!-- Example Wazuh custom rule placeholder -->
<group name="custom_iot_rules">
  <!-- Failed SSH login burst -->
  <!-- Tune 'frequency' and 'timeframe' after observing your baseline -->
  <rule id="100100" level="7">
    <if_sid>5710</if_sid>  <!-- Example: base failed SSH rule ID in Wazuh -->
    <field name="data.srcip">!192.168.0.0/16</field>
    <frequency>5</frequency>
    <timeframe>120</timeframe>
    <description>IoT: Multiple failed SSH logins within 2 minutes</description>
  </rule>
</group>
